import BottomNavComponent from "../layout/BottomNavComponent";

const FooterComponent = () => {
  return (
    <div className="w-full">
      <BottomNavComponent />
    </div>
  );
};

export default FooterComponent;
